{
  "name": "@harmoni/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && vite build",
    "dev": "concurrently \"pnpm --filter @harmoni/web dev\" \"wait-on http://localhost:3000 && electron .\"",
    "start": "electron .",
    "lint": "echo 'lint desktop'"
  },
  "dependencies": {
    "electron": "^33.2.0"
  },
  "devDependencies": {
    "wait-on": "^8.0.1",
    "@types/node": "^22.10.1",
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.1"
  }
}
